import { ContactoEmergencia } from './contacto-emergencia';

describe('ContactoEmergencia', () => {
  it('should create an instance', () => {
    expect(new ContactoEmergencia()).toBeTruthy();
  });
});
