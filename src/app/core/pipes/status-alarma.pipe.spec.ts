import { StatusAlarmaPipe } from './status-alarma.pipe';

describe('StatusAlarmaPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusAlarmaPipe();
    expect(pipe).toBeTruthy();
  });
});
