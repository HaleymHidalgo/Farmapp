import { TiempoAlarmaPipe } from './tiempo-alarma.pipe';

describe('TiempoAlarmaPipe', () => {
  it('create an instance', () => {
    const pipe = new TiempoAlarmaPipe();
    expect(pipe).toBeTruthy();
  });
});
